Use the existing storageService at:
@/shared/services/storage.service

It already uploads to the existing "media" bucket and returns:
{ path, publicUrl }

Review screenshots are stored under:
instagram-reviews/<generated-file-name>
